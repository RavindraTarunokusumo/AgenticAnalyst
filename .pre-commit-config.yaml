# Pre-commit configuration for AnalystEngine
# Add hooks once the project stack and linters are chosen.

repos:
  # Example (uncomment and adapt when tools are selected):
  # - repo: https://github.com/pre-commit/pre-commit-hooks
  #   rev: v4.6.0
  #   hooks:
  #     - id: trailing-whitespace
  #     - id: end-of-file-fixer
  #     - id: check-yaml
  #     - id: check-added-large-files
